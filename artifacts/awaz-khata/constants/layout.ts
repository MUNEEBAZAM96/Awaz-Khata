/**
 * Bottom tab bar sizing — shared between the tab layout and the chat
 * screen's keyboard offset so they can never drift apart.
 */
export const TAB_BAR_HEIGHT_WEB = 84;
export const TAB_BAR_CONTENT_HEIGHT = 58;
